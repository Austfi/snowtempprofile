# Decision Impact Summary (senator_beck, 2026-01-01 to 2026-01-31)

## What Changes The Most?
- `baseline_measured`: d|grad|>=20h `+0`, dT<=-15h `+0`, grad20 flips `0`h, cold15 flips `0`h.
- `sw_hourly_climatology`: d|grad|>=20h `+23`, dT<=-15h `+19`, grad20 flips `41`h, cold15 flips `57`h.
- `lw_hourly_climatology`: d|grad|>=20h `-51`, dT<=-15h `+24`, grad20 flips `175`h, cold15 flips `144`h.
- `lwdown_cloud_to_clear`: d|grad|>=20h `+95`, dT<=-15h `+107`, grad20 flips `95`h, cold15 flips `107`h.
- `airtemp_scale_0p0`: d|grad|>=20h `-33`, dT<=-15h `-9`, grad20 flips `95`h, cold15 flips `97`h.
- `wind_scale_0p0`: d|grad|>=20h `+191`, dT<=-15h `+159`, grad20 flips `195`h, cold15 flips `171`h.
- `air_temp_proxy`: d|grad|>=20h `-233`, dT<=-15h `-249`, grad20 flips `233`h, cold15 flips `251`h.

## Presentation Use
- Use `decision_impact_plot_*.png` first to show practical consequences in hours.
- Use `variable_importance_plot_*.png` second for technical cross-check.
- Use `mental_model_compare_*.png` last to bridge to practitioner education.
